# Advance-python-project
run via= npm run dev
you can just import the files donot have to add .js/.ts at end
swagger link=http://localhost:5000/api-docs
npm run start:dev   =>for  automatic restart if any change done